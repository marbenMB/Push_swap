#include "push_swap.h"

// void	push_swap(int ac, char **av)
// {
// 	t_list	*stack_a;

// 	stack_a = stock_elements(stack_a, ac, av);
// }

int	main(int ac, char **av)
{
	if (ac <= 1)
		error_no_args();
	check_args(ac, av);
	//push_swap(ac, av);
}